import pandas as pd

url = "https://raw.githubusercontent.com/akmand/datasets/master/iris.csv"
df = pd.read_csv(url)

print(df.head(2))
print(df.tail(3))
print(df.shape)
print(df.describe())
print(df.iloc[2:5])
df[df['species'] == 'setosa'].head(6)
